# Python 3.11 권장 / CUDA 12.6 기준
# 설치: pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu126 --extra-index-url https://pypi.org/simple

# PyTorch
torch==2.12.0+cu126
torchvision==0.27.0+cu126

# 딥러닝
transformers==5.16.1
accelerate
soxr

# RAG / 임베딩
sentence-transformers==6.0.1
faiss-cpu==1.15.0

# 영상 처리
opencv-python-headless==4.14.0.94
Pillow
yt-dlp
curl_cffi>=0.10  # yt-dlp의 TikTok 브라우저 impersonation에 필요 — 없으면 다운로드가 간헐적으로 실패함

# OCR
onnxruntime==1.29.0
rapidocr==3.9.2

# HuggingFace
huggingface_hub>=0.26.5

# API 서버
fastapi
uvicorn[standard]
python-multipart
