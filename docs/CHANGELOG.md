# CHANGELOG — cybercop_pipeline_AdotX.py → cybercop_pipeline_AdotX_v0_1.py

`cybercop_pipeline_AdotX_v0_1.py`는 `detection_ocr/cybercop_pipeline_v3.py`(CoT 분류 + Grounding DINO)를
저장소 루트로 이전하고, 상대경로만 루트 기준으로 조정한 파일입니다. 로직 자체는 원래의 `detection_ocr/` 버전과
동일합니다 (루트 배포 후 `detection_ocr/`의 중복 사본과 `pipeline/` 서브모듈은 삭제됨 — `app/main_v0_1.py`도
이제 루트 파일을 직접 import).

아래는 기존 `cybercop_pipeline_AdotX.py`(v0.1, 레거시) 대비 무엇이, 왜 바뀌었는지 정리입니다.

## 1. 분류(사기/정상 판단) 방식이 완전히 바뀜

- **이전(AdotX.py)**: 프레임마다 VLM을 1번씩 호출해 OCR 텍스트 + 객체를 동시에 추출하고, 그 결과를 RAG로 검색해서
  **최고 유사도 ≥ 0.5면 abnormal**로 자동 판정. 별도의 "판단" 단계가 없고 RAG 매칭 자체가 곧 판정.
- **이후(AdotX_v0_1.py)**: 2단계 CoT로 분리.
  1. 대표 프레임 4장을 grid 이미지 1장으로 합쳐 VLM에 "판단하지 말고 관찰한 것만 서술하라"고 시킴 (묘사만)
  2. 묘사 텍스트를 VLM(텍스트 전용, 이미지 없음이라 5~10배 빠름)에 다시 넣어 8개 체크리스트 기준으로 "사기/정상" 판단
  - 이미지를 보면서 동시에 논리 규칙을 적용하는 걸 VLM이 어려워해서 "보는 것"과 "판단하는 것"을 분리함

## 2. Grounding DINO 증거 탐지 + 에스컬레이션 추가 (완전히 새로운 기능)

- **이전**: 없음. 결과는 abnormal/normal 2-state.
- **이후**: `IDEA-Research/grounding-dino-tiny`로 37개 사기 특화 어휘(`pipeline/vocab.py`의 `SCAM_EVIDENCE_VOCAB` —
  계좌 화면, 카카오톡 화면, 신분증, 검찰청 공문서, QR코드 등)를 영상 전체 프레임(균등 샘플링, 최대 15장)에서 bbox 탐지.
  탐지 결과를 `evidence_score`(0~1)로 집계해서, **CoT가 "정상"이라 판단해도 evidence_score ≥ 0.15면 "검토필요"로
  강제 재분류**. 결과 상태가 2-state(abnormal/normal)에서 **3-state(사기/정상/검토필요)** 로 확장됨.
- DINO는 판단 결과와 무관하게 **모든 영상에서 항상 실행**됨 (정상 판정을 놓친 False Negative를 잡기 위한 안전망이라
  정상 판정된 영상에서도 돌려봐야 의미가 있음).

## 3. "정상" 확정 영상은 상세 분석을 스킵 (효율성)

- **이전**: 모든 영상에 대해 프레임별 OCR+객체 추출을 항상 수행.
- **이후**: 1단계(CoT) + 2단계(DINO 에스컬레이션)로 최종 판정이 "정상"으로 확정되면, OCR/객체 추출/RAG 검색을 전부
  생략하고 즉시 종료. "사기" 또는 "검토필요"인 영상만 3단계(상세 분석)까지 진행.

## 4. 객체 인식 소스가 바뀜 (VLM 자유서술 → DINO + VLM 폴백 병행)

- **이전**: 매 프레임마다 VLM이 "사물을 한국어로 자유롭게 나열"(최대 10개) → `ObjectMapper`가 임베딩 유사도로
  `allowed_objects.json`(439종 고정 클래스)에 매핑.
- **이후(현재)**: DINO 결과(`evidence.top_labels`, 좁고 정밀한 35종 사기 증거 어휘)와 VLM 자유서술 폴백 결과
  (`ObjectMapper`로 `allowed_objects.json` 439종에 매핑, threshold 0.7)를 **항상 둘 다 실행해서 합집합으로 합침**
  — DINO는 정밀하지만 좁은 어휘라 놓치는 게 많고 VLM은 넓지만 부정확할 수 있어서, 사기/검토필요로 확정된 영상은
  증거를 최대한 남기기 위해 서로 보완시킴 (정상 영상은 3단계 자체를 스킵하므로 비용 문제 없음).
  이 구조가 자리잡기까지 아래 문제들이 순차적으로 발견되어 수정됨:
    1. 폴백 트리거 조건이 원래 `n_detected_frames==0`(DINO 실패)만 체크해서, "OCR도 없고 DINO가 틀린 걸 찾은" 케이스를
       못 잡음 → `or not ocr_spans` 조건 추가 (이후 병행 실행 구조로 바뀌면서 이 트리거 조건 자체는 제거됨)
    2. 폴백 함수 내부에 `>=2` 프레임 다수결 투표 필터가 남아있어서, 폴백(최후의 보루) 상황에서도 결과가 빈 리스트가 됨
       → 투표 필터 제거
    3. **VLM이 `[OBJECTS]` 형식 태그를 프롬프트 지시대로 안 붙이고 콤마 리스트만 뱉는 경우가 있어서, 파싱 정규식이
       태그를 못 찾아 매번 빈 리스트를 반환**하고 있었음 → 태그 없으면 raw 텍스트 전체를 그대로 파싱하도록 완화
       (이게 없었으면 폴백 로직 자체가 사실상 죽어있는 코드였음)
    4. DINO가 탐지는 했지만 애매한 것(예: `avg_score=0.41`짜리 "screen", `0.50`짜리 "명품 로고")만 있는 경우,
       그게 그대로 `top_labels`에 들어가 RAG 쿼리를 오염시켜 완전히 엉뚱한 범죄유형과 매칭되는 문제 발견
       (실사례: 주식 투자 사기 영상이 "명품 로고" 객체 때문에 "사이버 도박"/"경마"로 매칭됨)
       → `evidence_aggregator.py`에 `MIN_LABEL_SCORE` 신뢰도 컷 추가, `top_labels`는 이 기준을 통과한 것만 포함.
       처음엔 evidence_score(에스컬레이션 판단)는 이 필터 없이 그대로 관대하게 유지했으나, 그 경계선 오탐들이
       evidence_score를 끌어올려 **정상 영상이 검토필요로 잘못 에스컬레이션되는 문제**가 실제로 발생함
       (실사례: 액체질소 시연 영상이 "오픈채팅방" 0.50, "휴대폰 화면" 0.54 오탐으로 검토필요 판정됨)
       → `MIN_LABEL_SCORE`를 0.5 → **0.55**로 올리고 `evidence_score` 계산에도 동일하게 적용해서 통일. 근본적으로
       너무 일반적이라 사기 여부와 무관하게 항상 잡히던 `SCAM_EVIDENCE_VOCAB`의 "컴퓨터 화면"/"휴대폰 화면" 두
       항목은 threshold로 못 거르는 문제라 어휘에서 아예 제거 (35개로 축소)
    5. VLM 폴백이 자유 서술한 단어를 `ObjectMapper`가 `allowed_objects.json`에 강제 매핑하는 과정에서, 어휘에
       없는 개념이 발음/글자 형태가 비슷한 무관한 단어로 잘못 매핑되는 문제 발견 (실사례: "금고"→"망고" 0.63,
       "저장"→"컴퓨터" 0.62, "이모티콘"→"리모컨" 0.62) → `ObjectMapper` threshold 0.6 → **0.7**로 상향
       (확인된 오매핑은 전부 걸러지고, "앱"→"텔레그램 앱" 0.73, "확인"→"인증" 0.77 같은 정상 매핑은 유지됨)
    6. `data/thecheat.csv`의 27개 영상 전체에서 VLM 폴백 원본 출력을 모아 빈도 집계
       (`data/vlm_fallback_object_frequency.csv`)해서 어휘 보강 후보를 찾음. 빈도만으로 판단하면 안 되는 걸 확인함 —
       (a) 표본이 27개(전부 사기 라벨)뿐이라 사기와 무관한 일반 소품(옷/색상/몸부위)도 우연히 여러 번 겹치고,
       (b) 색상·속도 수식어를 안 걸러서 같은 개념이 "채굴기"/"광속 채굴기"/"급속 채굴기"처럼 여러 변형으로 쪼개져
       원본 빈도수가 실제보다 낮게 잡힘 → 사람이 직접 보고 같은 개념끼리 묶고 동사/대명사/고유명사/가격 등
       물체가 아닌 것들을 골라내는 수작업 큐레이션 진행. 결과로 `SCAM_EVIDENCE_VOCAB`에 "포인트 적립 화면" 1개
       추가(35→36개, 리퍼럴/포인트 적립형 앱 사기가 기존 어휘로 전혀 안 잡히던 걸 발견해서 추가),
       `allowed_objects.json`에 29개 추가.
    7. 이번 검토 중에 `allowed_objects.json`에 이전부터(이번 추가분과 무관하게) 있던 문제도 발견함 —
       "화면 하단"/"화면 중앙"/"화면 오른쪽 상단"(물체가 아니라 화면 위치), "주식"/"주가"/"주식 가격"/
       "주식 거래"/"주식 정보"(개념 하나가 여러 유사어로 쪼개짐), "문구류"(중복), "메시지 목록"(중복),
       "틱톡 로고"(중복) 등 8개를 확인해서 제거 (446 → 475 → 467).
    8. 이어서 한 번 더 검토해서 "계산대 위 모니터"/"차량 내부"/"건물 외벽"(지나치게 구체적인 장면 묘사, 물체
       카테고리 아님), "화면 오른쪽 상단" 등 나머지 화면-위치 표현, "가짜 의사"(물체 아닌 상황 묘사),
       "금액"/"가격 정보"/"거래량"/"숫자"/"날짜"/"시간 표시"(추상적 수치·시간 데이터), "인증"/"문구"/"내역"/
       "리뷰"/"인터뷰 내용"(추상적 텍스트·콘텐츠 개념), "안내문"(안내 문구와 중복), "모니터"(컴퓨터/컴퓨터 화면과
       중복), "프로그램 로고"(로고와 중복), "이름"(추상적 텍스트 라벨), "티셔츠"(셔츠와 중복), "틱톡"/"MSN 메신저"
       (물체가 아닌 브랜드명 단독) 총 18개를 추가로 제거 (467 → **439**). 앞쪽 원본 Object365 목록(~380개)은
       아직 전체 재검토 안 함 — 비슷한 문제가 더 있을 수 있음

## 5. OCR 프레임 간 병합 방식이 바뀜

- **이전**: 연속된 프레임의 OCR 텍스트 블록 전체를 `difflib.SequenceMatcher`로 비교해서 유사도(≥0.5)가 높으면
  이어붙이는 순차적 병합.
- **이후**: 텍스트 bbox 위치(IoU ≥ 0.3) + 문자열 유사도(CER ≤ 0.5)를 함께 봐서 Union-Find로 "같은 화면 요소"를
  프레임 간에 클러스터링하고, 클러스터 안에서 가장 많이 등장한 텍스트 변형을 다수결로 채택. 호가창 숫자처럼 실제로
  매 프레임 바뀌는 값은 클러스터링 대상에서 제외(`is_numeric_only`).

## 6. OCR 텍스트 VLM 교정 단계

- v3를 처음 포팅했을 때는 이 단계가 빠져 있었으나, 원본 `postprocess_ocr_with_vlm`(오타/할루시네이션 교정, 끊어진
  문장 연결, @아이디·URL·전화번호·계좌번호 등 식별자는 원문 보존)을 그대로 다시 포팅해 넣음. 클러스터링된 OCR
  타임라인 전체를 VLM에 한 번에 넣어 교정 — 프레임별이 아니라 영상당 1번 호출.

## 7. 결과 JSON 스키마 차이

| 이전 (`AdotX.py`) | 이후 (`AdotX_v0_1.py`) |
|---|---|
| `label` (GT) | `label` (GT) |
| — | `cls_label` (1단계 CoT 판단) |
| `label`과 동일한 자리에 판정값 저장 안 함(파일명이 `label`) | `final_label` (DINO 에스컬레이션까지 반영한 최종 판정) |
| — | `classify_summary` (묘사 + 판단근거) |
| — | `grounding` (DINO 라벨별 count/frame_ratio/avg_score, evidence_score, top_labels) |
| `objects` | `objects` (소스가 DINO 우선으로 바뀜, 위 4번 참고) |
| `ocr_frames` | `ocr_before` (역할 동일 — 프레임별 원본) |
| `ocr_merged` | `ocr` (역할 동일 — 병합/교정 후 최종본, 클러스터링 방식이 다름) |
| — | `ocr_text` (`ocr`의 모든 스팬을 `" \| "`로 합친 문자열 하나 — RAG 쿼리에 실제로 들어가는 값을 그대로 노출) |
| — | `ocr_candidates` (클러스터별 후보 텍스트 변형 top-3, dominant_share 등 상세 정보) |
| `rag` | `rag` (동일하게 사용, 다만 "정상" 확정 영상은 아예 비어있음 — 3번 참고) |

## 8. 모델 호출 비용

- **이전**: 영상당 VLM 호출 = 프레임 수 × 1(OCR+객체 동시) + 1(OCR 교정). 프레임이 많을수록 선형 증가.
- **이후**: "정상" 확정 영상 → VLM 2회(묘사 1 + 판단 1)만 호출, 프레임 수와 무관. "사기/검토필요" 영상 → 여기에
  OCR 교정 1회, 그리고 VLM 폴백 프레임별 객체나열 호출이 항상 추가(위 4번 참고, DINO 결과 유무와 무관하게 실행).
  DINO는 프레임 수와 무관하게 가벼운 별도 모델로 항상 실행.

## 9. 로컬 파일(`--file`/`--dir`) 입력 지원 — 레거시 전용 기능이었던 걸 포팅

- **이전**: `--file`/`--dir`(로컬 영상/이미지 직접 테스트, 다운로드 없이)은 레거시 `cybercop_pipeline_AdotX.py`에만
  있고 `AdotX_v0_1.py`엔 `--url`/`--csv`만 있었음.
- **이후**: `AdotX_v0_1.py`에도 동일하게 포팅함. `process_single_video`(URL)에서 프레임 추출 부분(`_get_video_frames`)과
  분석·저장 부분(`_analyze_and_save`)을 분리해서, 로컬 파일 전용 `process_local_file`이 다운로드 없이 같은 분석
  로직을 재사용하도록 리팩터. 이미지는 시간 축이 없어서 `_get_image_frames`로 프레임 1장을 1~3단계 모두에
  재사용(OCR 클러스터링 등 다중 프레임 전제 로직도 프레임 1개에서 정상 동작 확인).
  `app/main_v0_1.py`의 `POST /api/video/upload`도 같은 구조(`_get_video_frames`/`_get_image_frames`/
  `_analyze_frames`)로 맞춰서 이미지 업로드 지원 추가 (기존엔 이미지 업로드 시 400 에러로 막혀 있었음).
- 겸사겸사 `app/main_v0_1.py`에 하드코딩돼 있던 `CUDA_VISIBLE_DEVICES="0"`을 `setdefault`로 바꿔서, 0번 GPU가
  다른 프로세스로 차 있을 때 `export CUDA_VISIBLE_DEVICES=1`로 우회할 수 있게 함 (테스트 중 실제로 0번 GPU가
  다른 작업으로 꽉 차서 VLM 로딩이 `CUDA out of memory`로 죽는 걸 겪고 나서 고침).
